# Upgrade pip first (this must be done manually before installing requirements)
# Run separately: python -m pip install --upgrade pip

# Core ML libraries
torch==2.3.1+cpu      # CPU-only version of PyTorch (Make sure you use correct index URL when installing)
numpy==1.26.4

# YOLOv8 by Ultralytics
ultralytics==8.3.150

# Image and data processing
opencv-python>=4.8.0
Pillow>=10.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
pyyaml>=6.0

# Optional but helpful for performance
scipy>=1.10.0
requests>=2.31.0
