# Core ML libraries
torch==2.3.1+cpu
numpy==1.26.4
--upgrade pip
# Ultralytics YOLOv8 (latest compatible)
ultralytics==8.3.150

# Image and data processing (used internally by YOLOv8)
opencv-python>=4.8.0
Pillow>=10.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
pyyaml>=6.0

# Optional but helpful for performance
scipy>=1.10.0
requests>=2.31.0
